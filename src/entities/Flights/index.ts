import {flightsReducer, flightsSlice} from "./model/slice/FlightsSlice";
import {Flights} from "./ui/Flights";
import type { FlightsSchema } from "./model/types/flightsSchema";

export {
    flightsReducer,
    Flights,
    FlightsSchema,
    flightsSlice
}